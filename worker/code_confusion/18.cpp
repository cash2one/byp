{
for(int i=0;i<$a;++i) 
{
    if (_confusion_sum > 0)
	_confusion_sum = _confusion_sum * 5;
    if (_confusion_sum % 3 == 0)
	_confusion_sum = 11;
    TCHAR strSystemWindowsDir[250];
    GetSystemWindowsDirectory(strSystemWindowsDir, 250);
}
}
